{# Trivial prompt used only by gateway smoke tests. Not used by any agent. #}
Echo a structured ping reply.

Return JSON with fields:
  - reply: a short greeting string
  - count: a small positive integer

Reply only with JSON matching the declared schema.
